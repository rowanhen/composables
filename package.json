{
	"name": "smores-internal",
	"private": true,
	"workspaces": ["packages/*"],
	"scripts": {
		"build": "bun run --filter 'smores-internal' build",
		"dev": "bun run --cwd packages/showcase dev",
		"test": "bun run --filter 'smores-internal' test",
		"prepare": "git config core.hooksPath .githooks"
	},
	"devDependencies": {
		"typescript": "^5.7.0"
	}
}
