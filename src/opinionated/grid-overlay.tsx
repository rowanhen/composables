/**
 * Opinionated GridOverlay component.
 * Re-exports the primitive as the API is already clean.
 *
 * @example
 * ```tsx
 * <GridOverlay />
 * <GridOverlay color="rgba(0, 100, 255, 0.1)" />
 * ```
 */
export { GridOverlay, type GridOverlayProps } from '../_internal/grid-overlay'
