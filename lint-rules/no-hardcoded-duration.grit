language js

`"$value"` where {
  $value <: r"(^|\s)duration-\d+(\/|\s|$)",
  register_diagnostic(
    span = $value,
    message = "Hardcoded duration class detected. Use a motion token: duration-fast, duration-normal, duration-slow.",
    severity = "error"
  )
}
