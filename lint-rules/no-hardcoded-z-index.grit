language js

`"$value"` where {
  $value <: r"(^|\s)z-\d+(\/|\s|$)",
  register_diagnostic(
    span = $value,
    message = "Hardcoded z-index class detected. Use a semantic z-index token: z-dropdown, z-sticky, z-overlay, z-modal, z-popover, z-toast, z-tooltip.",
    severity = "error"
  )
}
