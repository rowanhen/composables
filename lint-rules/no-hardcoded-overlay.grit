language js

`"$value"` where {
  $value <: r"(^|\s)bg-black/\d+(\/|\s|$)",
  register_diagnostic(
    span = $value,
    message = "Hardcoded overlay background detected. Use bg-overlay token instead.",
    severity = "error"
  )
}
