/**
 * Opinionated Switch component.
 * Re-exports the primitive as the API is already clean.
 *
 * @example
 * ```tsx
 * <Switch checked={enabled} onCheckedChange={setEnabled} />
 * <Switch defaultChecked />
 * ```
 */
export { Switch } from '@/components/ui/switch'
