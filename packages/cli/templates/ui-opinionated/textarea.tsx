/**
 * Opinionated Textarea component.
 * Re-exports the primitive as the API is already clean.
 *
 * @example
 * ```tsx
 * <Textarea placeholder="Enter your message..." />
 * <Textarea rows={5} autoSize />
 * ```
 */
export { Textarea } from '@/components/ui/textarea'
