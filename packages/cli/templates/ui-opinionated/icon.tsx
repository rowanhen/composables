/**
 * Opinionated Icon component.
 * Re-exports the primitive as the API is already clean.
 *
 * @example
 * ```tsx
 * import { CheckIcon } from 'lucide-react'
 *
 * <Icon size="lg" color="primary">
 *   <CheckIcon />
 * </Icon>
 * ```
 */
export { Icon, iconVariants } from '@/components/ui/icon'
