/**
 * Opinionated Input component.
 * Re-exports the primitive as the API is already clean.
 *
 * @example
 * ```tsx
 * <Input placeholder="Enter your name" />
 * <Input type="email" disabled />
 * ```
 */
export { Input } from '@/components/ui/input'
