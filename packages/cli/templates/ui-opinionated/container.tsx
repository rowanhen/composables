/**
 * Opinionated Container component.
 * Re-exports the primitive as the API is already clean.
 *
 * @example
 * ```tsx
 * <Container size="md">
 *   <p>Centered content with max-width constraint</p>
 * </Container>
 * ```
 */
export { Container, type ContainerProps, containerVariants } from '@/components/_internal/container'
