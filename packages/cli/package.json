{
	"name": "@leitware/composables-cli",
	"version": "0.3.0",
	"description": "Composables — extended shadcn/ui-style copy-paste component library for React with a CLI installer",
	"type": "module",
	"bin": {
		"composables": "dist/cli.js"
	},
	"files": ["dist", "templates"],
	"scripts": {
		"build": "tsc -p tsconfig.json",
		"prepublishOnly": "bun run build",
		"dev": "bun run src/cli.ts",
		"test": "bun test"
	},
	"dependencies": {
		"commander": "^12.1.0",
		"picocolors": "^1.1.0"
	},
	"devDependencies": {
		"@types/node": "^22.0.0",
		"typescript": "^5.7.0"
	},
	"engines": {
		"node": ">=18.0.0"
	},
	"repository": {
		"type": "git",
		"url": "https://github.com/rowanhen/composables"
	},
	"publishConfig": {
		"access": "public"
	}
}
